
```ts
import { GetStaticPaths, GetStaticProps } from "next";
import fs from "fs";
import path, { join } from "path";

export default function FanMaPage(props: any) {
  return (
    <div>
      <h1>{props.title}</h1>
      <div>{props.fileName}</div>
    </div>
  );
}

export const getStaticPaths: GetStaticPaths = async () => {
  return {
    paths: [
      { params: { slug: ["path", "to", "page", "5.html"] } },
      { params: { slug: ["another", "path", "1.html"] } },
      { params: { slug: ["another", "path", "8.html"] } },
      { params: { slug: ["another", "path", "9.html"] } },
    ],
    fallback: 'blocking',
  };
};

export const getStaticProps: GetStaticProps = async ({ params }) => {
  const fileName = params?.slug?.toString().split(",").join("/").replace(/\.html$/, "");

  console.log(`文章路径：${JSON.stringify(`${fileName}.html`)}`);

  const fanmaPath = path.join(process.cwd(), 'fanma', `${fileName}`)

  return {
    props: {
      title: fileName,
      filename: `585.html`,
      html: { filename: JSON.stringify(`${fileName}.html`) },
      fileSystemWriteParams: {
        directory: `./58`,
        fileName,
      },
    },

  };
};

```


```ts
import { GetStaticPaths, GetStaticProps } from "next";
import fs from "fs";
import path, { join } from "path";

export default function FanMaPage(props: any) {
  return (
    <div>
      <h1>555</h1>
      <div>555</div>
      <div>{ props.fileName }</div>
    </div>
  );
}

export const getStaticPaths: GetStaticPaths = async () => {
  return {
    paths: [
      { params: { slug: ["path", "to", "page", "5.html"] } },
      { params: { slug: ["another", "path", "1.html"] } },
      { params: { slug: ["another", "path", "8.html"] } },
      { params: { slug: ["another", "path", "9.html"] } },
    ],
    fallback: false,
  };
};

export const getStaticProps: GetStaticProps = async ({ params }) => {
  // 将'slug'数组的最后一个元素转换为字符串
  let fileName = params.slug[params.slug.length - 1].toString();

  // 检查文件名是否以'.html'结尾
  if (fileName.endsWith(".html")) {
    fileName = fileName.slice(0, -5);
  }
  console.log("File name without extension:", fileName);
  return {
    props: { 
      fileName,
      fallback: {
        path: fileName,
        // 或者使用数字（Next.js 13 或更高版本）
        // 确保提供的数字少于 getStaticPaths 中定义的所有路径的数量，否则可能会导致构建失败
        // fallback: 5
    }
    },

  };
}

```
